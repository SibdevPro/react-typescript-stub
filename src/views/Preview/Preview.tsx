import { FC } from 'react';

import { Preview as PreviewComponent } from 'src/components';

// TODO: Delete this before development
const Preview: FC = () => {
  return <PreviewComponent />;
};

export default Preview;
